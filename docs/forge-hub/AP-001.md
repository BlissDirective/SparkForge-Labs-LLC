APPROVAL PACKET AP-001 — Accounts and platforms for Forge Hub Day 1
Gate: P0   Tier: 2   Requested by: Foreman (SparkForge-Labs CDO covering)
What: Owner decisions from TRANSITION_ACTION_PLAN v2.2 §11.6 that unblock Smith Track A, LFS, device QA, and agent GitHub access. No product UI changes.
Why now: Day 1 dispatch (§11.9) waits on these for mesh-gen, LFS, and device lab; everything else can start without them.
Look: n/a (accounts only)
Diff: none yet
Risk and rollback: Creating accounts incurs spend; reject leaves Track A and device lab paused; Track B / physical Chromebook remain options.
Options considered:
A (recommended): Enable Git LFS on this repo (2 GB); create Meshy or Tripo3D with smallest credit pack → `MESH_GEN_API_KEY`; owner Mixamo Adobe account; prefer one physical Chromebook (+ iPad later) over BrowserStack for now; grant agents fine-grained PAT/App for this repo only.
B: Skip mesh-gen (Track B artist only); LFS yes; no device lab yet (CI Chromium/WebKit only).
C: Defer all §11.6 spends two weeks; engineering continues on placeholder Sparky + room shell only.
Ask: Approve | Revise | Reject

### Owner answer — 2026-09-15: **Approve, Option A** ("I approve all pending actions").

What that unblocks, and who does the remaining hands-on step:

| Item | State after approval | Hands-on step (owner only unless noted) |
|---|---|---|
| Stray `main` | Deleted by the owner 2026-09-15 | Done |
| Branch protection on `setup-sparkforge-dev` | Approved | Owner applies in GitHub: Settings → Branches → Add rule for `setup-sparkforge-dev`: require a pull request before merging (1 approval, dismiss stale approvals), require status checks to pass (`Typecheck, test, build`, `Secret scan (gitleaks)`, `RLS verification`, `E2E smoke (Playwright)`, `SSIM vs LOCKED_HERO.png` when it has run), require branches to be up to date, block force pushes, do not allow bypass. Agents cannot change repository settings. |
| Git LFS on this repo | Approved; `.gitattributes` committed 2026-09-15 for `.blend`, `.fbx`, `.abc`, `.psd`, `.kra`, `.spp`, `.sbsar`, and `assets/**` EXR/TIF | Contributors run `git lfs install` once. GitHub LFS storage is on by default (1 GB free; buy a data pack if the character sources exceed it). |
| Mesh-gen account (Meshy or Tripo3D) | Approved, smallest credit pack | Owner creates the account, checks its terms on ownership of generated output, stores the key as `MESH_GEN_API_KEY` in the agent runner's secret store (never in the repo). Smith's W3-02 starts the moment the key exists. |
| Mixamo (free Adobe) | Approved | Owner creates; browser use only, no key. |
| Device lab | Approved: one physical Chromebook (Intel Iris Xe class) first, iPad later | Owner purchases; Inspector records it in `REFERENCE_HARDWARE.md` §2. Until then the CI SwiftShader baseline is the only measured number and gate packets say so. |
| Contract artist (Track B) | Approved to brief now; hire decision at C1 with Track A costed alongside | Owner hires from `docs/sparky/ARTIST_BRIEF.md` (Smith writes it, W3-01). |
| Agent GitHub access | Approved: fine-grained PAT or GitHub App scoped to this repo, Contents + Pull requests read/write, Actions read, no admin | Owner creates; stored as `GITHUB_TOKEN` in the runner secret store. |

- [x] GitHub: delete stray `main` (done 2026-09-15); protect `setup-sparkforge-dev` — owner to apply the rule above
- [x] Git LFS on this repo — `.gitattributes` committed
- [x] Mesh-gen account + spend — approved; owner creates and stores the key
- [x] Mixamo (free Adobe) — approved; owner creates
- [x] Device lab: physical Chromebook — approved; owner purchases
- [x] Contract artist Track B: brief now, hire at C1 — approved
