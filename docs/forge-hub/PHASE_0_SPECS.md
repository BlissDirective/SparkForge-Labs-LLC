# Phase 0 — Spec lock (LOCKED)

**Date:** 2026-09-14  
**Status:** **LOCKED** — adopts `TRANSITION_ACTION_PLAN.md` v2.2 §0 decisions 1–13  
**Supersedes:** 2026-09-12 draft defaults (overlay-only Sparky, games EXIT) where they conflict  
**Branch:** `setup-sparkforge-dev` → also `docs/forge-hub/`

Owner decisions 1–13 in the Transition Action Plan are closed. Agents may propose; they may not reopen.

## Summary of locked answers
1. Forge hub = kid-facing shell on desktop/ultrawide (Concept 10 / Rebuild IV amended, not ignored)
2. DOM panels projected from 3D glass (content never stretches)
3. One canvas in root layout; FLAT routes pause/hide it
4. No canvas below 1440 px; HTML shell restyled for compact
5. Games play in merged `PlayStage` by default; per-game fullscreen hatch
6. Sparky = rigged 3D on desk + head HoloBubble chat (no painted dock on plate)
7. Welcome = shrunk sides (hero details) + center login; `/` `/login` share scene
8. Director choreography (GSAP + Theatre.js)
9. PR #164: port then close
10. `three/webgpu` via `createRenderer`; `FORGE_HUB*` flags
11. Marketing long-scroll dropped
12. Year-one outfit catalog (§6b)
13. Sparky conversation = head-emitter HoloBubble

## Exit → P1
W0 governance in flight; `/dev/forge-hub` room shell next (Stagehand). See `TASK_BOARD.md`.
